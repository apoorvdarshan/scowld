import SwiftUI

// MARK: - Amica Theme Colors

extension ShapeStyle where Self == Color {
    /// Amica's signature blue — matches her hair color
    static var amicaBlue: Color {
        Color(red: 0.25, green: 0.45, blue: 0.85)
    }
}

extension Color {
    /// Amica's signature blue — matches her hair color
    static let amicaBlue = Color(red: 0.25, green: 0.45, blue: 0.85)
}
