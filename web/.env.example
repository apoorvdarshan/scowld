# Scowld's website no longer requires hosted AI, speech, billing, or RevenueCat
# environment variables. Provider API keys are entered inside the iOS app and
# stored locally in the user's iOS Keychain.
